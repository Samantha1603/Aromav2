<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.aroma">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignUp"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".forgot_password"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".HomePage" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".SearchByTitle"></activity>
        <activity android:name=".SearchByIngredients"></activity>
        <activity android:name=".SearchPopular"></activity>
        <activity android:name=".RecipeDetails"></activity>
        <activity android:name=".FoodList"></activity>
    </application>

</manifest>